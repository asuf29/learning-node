const hello = (name) => {
    console.log(`Hello ${name}`);
};

const sum = (a, b) => a + b;

const substract = (a, b) => a - b;

const text = "Text";

const user = {
    name: "Asude",
    surname: "Fışkın",
    age: 21,
};

const users = [{
    name: "Damla",
    surname: "Kasal",
    age: 21,
}, 
{   
    name: "Alp",
    surname: "Boyacıoğlu",
    age: 2,
}];

export {hello, sum, subtract, text, user, users};